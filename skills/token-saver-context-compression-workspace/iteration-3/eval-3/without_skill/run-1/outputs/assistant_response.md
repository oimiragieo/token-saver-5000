I don't have access to local filesystem tools (Bash, Glob, Grep, Read, etc.) in this environment â€” only Hugging Face MCP tools, LSP, and image generation are available.

Without filesystem access, I **cannot inspect the local repo** to answer your question about:

1. The exact command pattern for a LangChain-style JSON payload
2. The output-format policy
3. A cache-friendly prompt-structure note

Could you either:
- **Share the repo name/path** so I can look it up if it's on Hugging Face, or
- **Paste the relevant file contents** (e.g., a README, config, or script) into the chat so I can analyze them?