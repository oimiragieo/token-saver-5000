I don't have file system tools (Bash, Read, Glob, Grep, etc.) available in this session â€” only Hugging Face MCP tools, LSP, and image generation. I cannot inspect local files or the repo structure without those tools.

To answer your question properly, I'd need access to file-reading tools (like `Read`, `Bash`, or `Glob`) so I can:

1. Find the repo and its CLI/API for context compression.
2. Read the code to identify the "best query-targeted mode" option.
3. Determine the exact command syntax and whether the output is marked as safe to answer from.

Could you either:
- Re-run this with file system tools enabled, or
- Share the relevant source code / CLI help output so I can answer precisely?