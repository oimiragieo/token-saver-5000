I don't have access to local filesystem tools (Bash, Glob, Read, etc.) in this environment â€” only Hugging Face and LSP tools are available. I can't inspect the repo to find the exact command, Makefile targets, or scripts.

To answer your question, I'd need access to the local filesystem tools. Could you either:

1. Share the contents of the repo's `Makefile`, `package.json`, `pyproject.toml`, or equivalent task runner config, or
2. Enable the Bash/filesystem tools so I can explore the repo directly?