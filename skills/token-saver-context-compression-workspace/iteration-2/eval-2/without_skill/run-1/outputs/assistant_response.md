I don't have access to file system tools (Bash, Glob, Read, etc.) in this environment, so I cannot inspect the local repository to determine its structure, commands, or workflow.

To answer your question accurately, I need to be able to read the repo's files (e.g., `Makefile`, `package.json`, `pyproject.toml`, scripts, and the referenced fixture file). Could you either:

1. **Share the repo contents** â€” paste the relevant config/build files and any README or CLAUDE.md, or
2. **Enable file system tools** â€” so I can explore the repo directly.

Without seeing the actual repo, any answer I give would be a guess, which I want to avoid.