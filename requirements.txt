# Core MCP and AI dependencies
mcp>=0.9.0
sentence-transformers>=3.1.0  # Security: Fixes CVE-2024-11392/11393/11394 (arbitrary code execution)
networkx>=3.0
scikit-learn>=1.3.0
numpy>=1.24.0
torch>=2.0.0
pydantic>=2.0.0
tiktoken>=0.5.0

# Optional: ChromaDB for vector storage (Python 3.10-3.12 only, requires numpy<2.0)
# System automatically falls back to JSON storage if ChromaDB unavailable
# chromadb>=0.4.0  # INCOMPATIBLE with Python 3.13+ (requires numpy<2.0)

# Utilities
tqdm>=4.65.0
psutil>=5.9.0

# Development and Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
black>=23.0.0
ruff>=0.1.0
