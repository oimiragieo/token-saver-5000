Search all compressed documents for relevant information.

Steps:
1. Use `mcp__token-saver__list_documents` to see available documents
2. Use `mcp__token-saver__search_semantic` with the query below
3. Show top 5 results with:
   - Node ID
   - Document source
   - Relevance score (0-1)
   - Brief preview of the content
4. Offer to expand any section using `mcp__token-saver__modulate_region`

Query: $ARGUMENTS
